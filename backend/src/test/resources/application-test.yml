spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/realkorea
    username: realkorea
    password: realkorea1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
  data:
    redis:
      host: localhost
      port: 6379
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: dummy-client-id-for-test
            client-secret: dummy-client-secret-for-test
            scope:
              - email
              - profile

security:
  jwt:
    secret: test-secret-that-is-at-least-32-characters-long
    access-token-expiration-seconds: 900
    refresh-token-expiration-seconds: 1209600
